*
  box-sizing border-box

body
  margin 0
  height 100vh
  width 100vw
  color var(--color)
  background var(--bg)
  font-family sans-serif
  font-size 14px
  line-height 1
  user-select none
  overflow hidden

::selection
  background var(--theme)
  color var(--theme-color)
